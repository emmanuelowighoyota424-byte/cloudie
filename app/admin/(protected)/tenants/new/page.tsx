'use client'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
export default function NewTenantPage(){const router=useRouter();const [name,setName]=useState('');const [slug,setSlug]=useState('');const [ownerId,setOwnerId]=useState('');const [siteType,setSiteType]=useState('BANK');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/admin/tenants',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,slug,ownerId,siteType})});const d=await r.json();if(!r.ok)setError(d.error??'Unable to create tenant');else router.push(`/admin/tenants/${d.id}`);setBusy(false)}return <main className="mx-auto max-w-xl p-6"><p className="text-sm text-muted-foreground">Admin / Tenants</p><h1 className="mt-1 text-2xl font-semibold">Create tenant</h1><form onSubmit={submit} className="mt-6 space-y-4 rounded-xl border bg-background p-6"><label className="block text-sm">Tenant name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="block text-sm">Slug<input required value={slug} onChange={e=>setSlug(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="block text-sm">Owner user ID<input required value={ownerId} onChange={e=>setOwnerId(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="block text-sm">Tenant mode<select value={siteType} onChange={e=>setSiteType(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"><option>BANK</option><option>INVESTMENT</option></select></label>{error&&<p className="text-sm text-destructive">{error}</p>}<button disabled={busy} className="w-full rounded-lg bg-foreground px-4 py-2 text-sm text-background">{busy?'Creating…':'Create tenant'}</button></form></main>}
