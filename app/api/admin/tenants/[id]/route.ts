import { NextResponse } from 'next/server'
import { Prisma } from '@prisma/client'
import { requireSuperAdmin } from '@/lib/authorization'
import { prisma } from '@/lib/prisma'
const plans=['FREE','STARTER','BUSINESS','ENTERPRISE'] as const
const subscriptionStatuses=['ACTIVE','TRIALING','PAST_DUE','CANCELED','INCOMPLETE'] as const
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await requireSuperAdmin();const {id}=await params;const body=await request.json().catch(()=>null) as Record<string,unknown>|null;const action=typeof body?.action==='string'?body.action:'';const workspace=await prisma.workspace.findUnique({where:{id},select:{id:true,plan:true,subscriptionStatus:true}});if(!workspace)return NextResponse.json({error:'Tenant not found.'},{status:404});if(action==='plan'){const plan=typeof body?.plan==='string'?body.plan:'';const reason=typeof body?.reason==='string'?body.reason.trim():'';if(!plans.includes(plan as typeof plans[number])||reason.length<3)return NextResponse.json({error:'Valid plan and reason are required.'},{status:400});await prisma.$transaction(async tx=>{await tx.workspace.update({where:{id},data:{plan:plan as typeof plans[number]}});await tx.auditLog.create({data:{actorId:actor.id,workspaceId:id,action:'TENANT_PLAN_CHANGED',entity:'Workspace',entityId:id,metadata:{from:workspace.plan,to:plan,reason}}})});return NextResponse.json({ok:true,plan})}if(action==='subscription'){const status=typeof body?.status==='string'?body.status:'';const reason=typeof body?.reason==='string'?body.reason.trim():'';if(!subscriptionStatuses.includes(status as typeof subscriptionStatuses[number])||reason.length<3)return NextResponse.json({error:'Valid subscription status and reason are required.'},{status:400});await prisma.$transaction(async tx=>{await tx.workspace.update({where:{id},data:{subscriptionStatus:status as typeof subscriptionStatuses[number]}});await tx.auditLog.create({data:{actorId:actor.id,workspaceId:id,action:'TENANT_SUBSCRIPTION_CHANGED',entity:'Workspace',entityId:id,metadata:{from:workspace.subscriptionStatus,to:status,reason}}})});return NextResponse.json({ok:true,subscriptionStatus:status})}if(action==='member'){const memberId=typeof body?.memberId==='string'?body.memberId:'';const status=String(body?.status??'');const reason=typeof body?.reason==='string'?body.reason.trim():'';if(!memberId||!['ACTIVE','SUSPENDED'].includes(status)||reason.length<3)return NextResponse.json({error:'Valid member, status and reason are required.'},{status:400});const member=await prisma.workspaceMember.findFirst({where:{id:memberId,workspaceId:id},select:{id:true,status:true}});if(!member)return NextResponse.json({error:'Member not found.'},{status:404});await prisma.$transaction(async tx=>{await tx.workspaceMember.update({where:{id:memberId},data:{status:status as 'ACTIVE'|'SUSPENDED'}});await tx.auditLog.create({data:{actorId:actor.id,workspaceId:id,action:status==='SUSPENDED'?'TENANT_MEMBER_SUSPENDED':'TENANT_MEMBER_REACTIVATED',entity:'WorkspaceMember',entityId:memberId,metadata:{from:member.status,to:status,reason}}})});return NextResponse.json({ok:true,status})}if(action==='profile'){const siteType=body?.siteType==='INVESTMENT'?'INVESTMENT':'BANK';const primaryColor=typeof body?.primaryColor==='string'&&/^#[0-9a-fA-F]{6}$/.test(body.primaryColor)?body.primaryColor:'#111827';const siteTitle=typeof body?.siteTitle==='string'?body.siteTitle.trim().slice(0,120):null;const contactEmail=typeof body?.contactEmail==='string'?body.contactEmail.trim().slice(0,200):null;const existing=await prisma.$queryRaw<Array<{id:string}>>(Prisma.sql`SELECT "id" FROM "TenantProfile" WHERE "workspaceId"=${id} LIMIT 1`);if(existing[0])await prisma.$executeRaw(Prisma.sql`UPDATE "TenantProfile" SET "siteType"=${siteType},"primaryColor"=${primaryColor},"siteTitle"=${siteTitle},"contactEmail"=${contactEmail},"updatedAt"=CURRENT_TIMESTAMP WHERE "workspaceId"=${id}`);else await prisma.$executeRaw(Prisma.sql`INSERT INTO "TenantProfile" ("id","workspaceId","siteType","primaryColor","siteTitle","contactEmail") VALUES (${crypto.randomUUID()},${id},${siteType},${primaryColor},${siteTitle},${contactEmail})`);await prisma.auditLog.create({data:{actorId:actor.id,workspaceId:id,action:'TENANT_PROFILE_UPDATED',entity:'TenantProfile',entityId:id,metadata:{siteType,primaryColor,siteTitle,contactEmail}}});return NextResponse.json({ok:true})}if(action==='lock'){const lock=body?.locked===true;await prisma.$executeRaw(Prisma.sql`INSERT INTO "TenantProfile" ("id","workspaceId","lockedAt") VALUES (${crypto.randomUUID()},${id},${lock?Prisma.sql`CURRENT_TIMESTAMP`:Prisma.sql`NULL`}) ON CONFLICT ("workspaceId") DO UPDATE SET "lockedAt"=${lock?Prisma.sql`CURRENT_TIMESTAMP`:Prisma.sql`NULL`},"updatedAt"=CURRENT_TIMESTAMP`);await prisma.auditLog.create({data:{actorId:actor.id,workspaceId:id,action:lock?'TENANT_SITE_LOCKED':'TENANT_SITE_UNLOCKED',entity:'TenantProfile',entityId:id}});return NextResponse.json({ok:true,locked:lock})}if(action==='domain'){const hostname=typeof body?.hostname==='string'?body.hostname.trim().toLowerCase():'';if(!hostname||!hostname.includes('.'))return NextResponse.json({error:'Valid hostname required.'},{status:400});const token=crypto.randomUUID();const domainId=crypto.randomUUID();await prisma.$executeRaw(Prisma.sql`INSERT INTO "TenantDomain" ("id","workspaceId","hostname","verificationToken") VALUES (${domainId},${id},${hostname},${token}) ON CONFLICT ("hostname") DO NOTHING`);await prisma.auditLog.create({data:{actorId:actor.id,workspaceId:id,action:'TENANT_DOMAIN_ADDED',entity:'TenantDomain',entityId:domainId,metadata:{hostname}}});return NextResponse.json({ok:true,hostname,verificationToken:token,status:'PENDING'})}return NextResponse.json({error:'Invalid action.'},{status:400})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Operation failed.'},{status:500})}}
