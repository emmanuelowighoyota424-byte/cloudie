import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { requireSuperAdmin } from '@/lib/authorization'
import { Prisma } from '@prisma/client'

const json=(data:unknown,status=200)=>NextResponse.json(data,{status})
async function actor(){return requireSuperAdmin()}
async function audit(actorId:string,action:string,entity:string,entityId:string|null,metadata:Prisma.InputJsonValue={}){await prisma.auditLog.create({data:{actorId,action,entity,entityId,result:'SUCCESS',metadata}})}

export async function GET(req:Request,{params}:{params:Promise<{path:string[]}>}){try{const user=await actor();const {path}=await params;const [resource,id]=path;const url=new URL(req.url);const q=url.searchParams.get('q')||'';if(resource==='users'){if(id){const item=await prisma.user.findUnique({where:{id},include:{pointLedger:{orderBy:{createdAt:'desc'},take:100},shipmentsCreated:{orderBy:{createdAt:'desc'},take:25},orders:{orderBy:{createdAt:'desc'},take:25},auditActorLogs:{orderBy:{createdAt:'desc'},take:50}}});return item?json(item):json({error:'USER_NOT_FOUND'},404)}const items=await prisma.user.findMany({where:q?{OR:[{email:{contains:q,mode:'insensitive'}},{name:{contains:q,mode:'insensitive'}},{id:{contains:q}}]}:undefined,orderBy:{createdAt:'desc'},take:100});return json({items})}if(resource==='vendors'){const items=await prisma.vendor.findMany({where:q?{OR:[{name:{contains:q,mode:'insensitive'}},{category:{contains:q,mode:'insensitive'}}]}:undefined,include:{workspace:{select:{id:true,name:true}},products:{select:{id:true,name:true,active:true}}},orderBy:{createdAt:'desc'},take:100});return json({items})}if(resource==='shipments'){const items=await prisma.shipment.findMany({where:q?{OR:[{trackingId:{contains:q,mode:'insensitive'}},{origin:{contains:q,mode:'insensitive'}},{destination:{contains:q,mode:'insensitive'}}]}:undefined,include:{workspace:{select:{id:true,name:true}},events:{orderBy:{createdAt:'desc'},take:5}},orderBy:{createdAt:'desc'},take:100});return json({items})}if(resource==='documents'){const items=await prisma.document.findMany({where:q?{OR:[{name:{contains:q,mode:'insensitive'}},{mimeType:{contains:q,mode:'insensitive'}}]}:undefined,select:{id:true,name:true,mimeType:true,sizeBytes:true,createdAt:true,updatedAt:true,workspaceId:true,ownerId:true},orderBy:{createdAt:'desc'},take:100});return json({items})}if(resource==='transactions'){const items=await prisma.pointLedger.findMany({where:q?{OR:[{userId:{contains:q}},{description:{contains:q,mode:'insensitive'}},{reference:{contains:q,mode:'insensitive'}}]}:undefined,include:{user:{select:{id:true,name:true,email:true}}},orderBy:{createdAt:'desc'},take:200});return json({items})}if(resource==='audit'){const items=await prisma.auditLog.findMany({orderBy:{createdAt:'desc'},take:200});return json({items})}if(resource==='analytics'){const [users,shipments,orders,docs,workspaces,points]=await Promise.all([prisma.user.count(),prisma.shipment.count(),prisma.order.count(),prisma.document.count(),prisma.workspace.count(),prisma.pointLedger.aggregate({_sum:{amount:true}})]);return json({users,shipments,orders,documents:docs,workspaces,netPoints:points._sum.amount??0})}if(resource==='system-health'){const [users,failedJobs]=await Promise.all([prisma.user.count(),prisma.cloudieJob.count({where:{status:'FAILED'}})]);return json({database:{status:'healthy',checkedAt:new Date().toISOString()},application:{status:'healthy'},backgroundJobs:{status:failedJobs?'degraded':'healthy',failed:failedJobs},users})}if(resource==='pricing'){return json({items:[]})}return json({error:'NOT_FOUND'},404)}catch(e){return json({error:e instanceof Error?e.message:'FORBIDDEN'},e instanceof Prisma.PrismaClientKnownRequestError?400:403)}}

export async function POST(req:Request,{params}:{params:Promise<{path:string[]}>}){try{const admin=await actor();const {path}=await params;const [resource,id,action]=path;const body=await req.json().catch(()=>({}));if(resource==='users'&&id&&(action==='credit'||action==='debit')){const amount=Number(body.amount);const reason=String(body.reason||'').trim();const reference=body.reference?String(body.reference):null;if(!Number.isInteger(amount)||amount<=0||!reason)return json({error:'INVALID_INPUT'},400);const signed=action==='credit'?amount:-amount;const result=await prisma.$transaction(async tx=>{const latest=await tx.pointLedger.findFirst({where:{userId:id},orderBy:{createdAt:'desc'}});const current=latest?.balance??0;if(action==='debit'&&current<amount)throw new Error('INSUFFICIENT_POINTS');if(reference){const duplicate=await tx.pointLedger.findFirst({where:{userId:id,reference}});if(duplicate)throw new Error('DUPLICATE_OPERATION')}const entry=await tx.pointLedger.create({data:{userId:id,amount:signed,balance:current+signed,description:reason,reference}});await tx.auditLog.create({data:{actorId:admin.id,userId:id,action:action==='credit'?'POINTS_CREDITED':'POINTS_DEDUCTED',entity:'User',entityId:id,result:'SUCCESS',metadata:{amount,reason,reference}}});return entry},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable});return json({ok:true,entry:result})}if(resource==='vendors'&&id&&['approve','reject','suspend','reactivate'].includes(action)){const status={approve:'APPROVED',reject:'REJECTED',suspend:'SUSPENDED',reactivate:'APPROVED'}[action]!;const vendor=await prisma.vendor.update({where:{id},data:{status}});await audit(admin.id,`VENDOR_${action.toUpperCase()}`,'Vendor',id,{reason:body.reason||null});return json({ok:true,vendor})}if(resource==='tenants'&&id&&(action==='lock'||action==='unlock'))return json({error:'TENANT_MODEL_NOT_CONFIGURED'},501);return json({error:'NOT_FOUND'},404)}catch(e){return json({error:e instanceof Error?e.message:'OPERATION_FAILED'},400)}}

export async function PATCH(req:Request,{params}:{params:Promise<{path:string[]}>}){try{const admin=await actor();const {path}=await params;const [resource,id]=path;const body=await req.json();if(resource==='users'&&id){const data:Record<string,unknown>={};if(body.status==='SUSPENDED')data.suspendedAt=new Date();if(body.status==='ACTIVE')data.suspendedAt=null;if(['USER','ADMIN','SUPER_ADMIN'].includes(body.role))data.role=body.role;const user=await prisma.user.update({where:{id},data});await audit(admin.id,body.status==='SUSPENDED'?'USER_SUSPENDED':body.status==='ACTIVE'?'USER_REACTIVATED':'USER_ROLE_CHANGED','User',id,{status:body.status||null,role:body.role||null});return json({ok:true,user})}return json({error:'NOT_FOUND'},404)}catch(e){return json({error:e instanceof Error?e.message:'OPERATION_FAILED'},400)}}
