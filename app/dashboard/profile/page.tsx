import { requireUser } from '@/lib/authorization'
import { prisma } from '@/lib/prisma'
export const dynamic='force-dynamic'
export default async function ProfilePage(){const user=await requireUser();const memberships=await prisma.workspaceMember.findMany({where:{userId:user.id},include:{workspace:true},orderBy:{createdAt:'asc'}});const sessions=await prisma.session.count({where:{userId:user.id}});return <div className="space-y-6"><div><p className="text-sm text-muted-foreground">Personal</p><h1 className="text-2xl font-semibold">Profile & security</h1></div><div className="rounded-xl border bg-background p-6 space-y-3 text-sm"><p><b>Name:</b> {user.name}</p><p><b>Email:</b> {user.email}</p><p><b>Email verified:</b> {user.emailVerified?'Yes':'No'}</p><p><b>Account role:</b> {user.role}</p><p><b>Active sessions:</b> {sessions}</p><p><b>Account status:</b> {user.suspendedAt?'Suspended':'Active'}</p></div><div className="rounded-xl border bg-background p-6"><h2 className="font-medium">Workspace membership</h2><div className="mt-3 divide-y">{memberships.map(m=><div key={m.id} className="flex justify-between py-3 text-sm"><span>{m.workspace.name}</span><span>{m.role} · {m.status}</span></div>)}</div></div></div>}
