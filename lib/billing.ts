import { Prisma } from '@prisma/client'
import { prisma } from '@/lib/prisma'
import { debitPoints } from '@/lib/points'
export async function chargeForAction(input:{userId:string;workspaceId:string;action:string;description:string;reference:string}){const rows=await prisma.$queryRaw<Array<{pointCost:number}>>(Prisma.sql`SELECT "pointCost" FROM "PointPricingRule" WHERE "action"=${input.action} AND "enabled"=TRUE AND ("workspaceId"=${input.workspaceId} OR "workspaceId" IS NULL) AND "effectiveFrom"<=CURRENT_TIMESTAMP AND ("effectiveTo" IS NULL OR "effectiveTo">CURRENT_TIMESTAMP) ORDER BY CASE WHEN "workspaceId"=${input.workspaceId} THEN 0 ELSE 1 END,"effectiveFrom" DESC LIMIT 1`);if(!rows[0])throw new Error(`No active pricing rule for ${input.action}`);const amount=Number(rows[0].pointCost);if(!Number.isSafeInteger(amount)||amount<0)throw new Error('Invalid pricing configuration');if(amount===0)return{amount:0,idempotent:false};const result=await debitPoints({userId:input.userId,amount,description:input.description,reference:`${input.reference}:workspace:${input.workspaceId}`,workspace:input.workspaceId});return{amount,idempotent:result.idempotent}}
